// All GameQueueT implementation lives in game_queues.h (header-only template).
//
// This file is intentionally empty but kept so the CMake target keeps a
// translation unit and existing build files don't need to be changed.

#include "self_play/game_queues.h"
